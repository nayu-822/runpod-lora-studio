# RunPod LoRA Studio コーディングルール

- 文書版数: 1.0
- 対象: Python、Gradio、シェルスクリプト、設定ファイル、テスト

## 1. 基本方針

- 可読性、保守性、安全性、再現性を優先する。
- 1つの関数・クラス・モジュールに複数の責務を持たせない。
- Gradio UIと業務処理を分離する。
- 外部サービス固有処理はアダプターへ閉じ込める。
- 原画像、元タグ、実行設定を破壊的に上書きしない。
- 長時間処理は別プロセスまたはジョブ管理層で実行する。
- 不明な例外を握りつぶさない。
- 実装と同時にテストを追加する。

## 2. Python

### 2.1 バージョン・形式

- Python 3.11以上を対象とする。
- `src`レイアウトを使用する。
- UTF-8を使用する。
- 公開関数・公開メソッドには型注釈を付ける。
- `from __future__ import annotations`を原則使用する。
- `pathlib.Path`を使用し、パスを文字列連結しない。
- `datetime`はタイムゾーン付きで扱う。
- 内部識別子にはUUIDを使用する。

### 2.2 命名

- モジュール・変数・関数: `snake_case`
- クラス・例外: `PascalCase`
- 定数: `UPPER_SNAKE_CASE`
- private要素: `_leading_underscore`
- bool変数: `is_`, `has_`, `can_`, `should_`, `enable_`等で始める
- 曖昧な名前（`data`, `info`, `tmp`, `obj`）を広いスコープで使わない

### 2.3 関数・クラス

- 関数は原則50行以内を目安にする。
- 引数が多い場合はdataclassまたはPydanticモデルへまとめる。
- 戻り値が複雑な場合は辞書ではなく型付きモデルを使う。
- 副作用を持つ関数名は動詞で始める。
- 状態変更と問い合わせを同じ関数に混在させない。
- 例外を返り値の`None`だけで表現しない。

### 2.4 データモデル

- ドメインモデルはUIコンポーネントへ依存させない。
- 永続化モデルと画面表示用モデルを必要に応じて分離する。
- 状態値は文字列直書きではなくEnumを使用する。
- JSON/TOMLに保存する設定には`schema_version`を持たせる。
- 学習Runは実行時設定のスナップショットを保持する。

## 3. アーキテクチャ

### 3.1 レイヤー

```text
UI (Gradio)
  ↓
Application Service
  ↓
Domain / Job Management
  ↓
Adapters / Persistence / External Processes
```

依存方向を逆転させない。`services`や`domain`からGradioをimportしない。

### 3.2 アダプター化対象

少なくとも以下をインターフェース化する。

- `ImageSourceAdapter`
- `TaggerAdapter`
- `TrainerAdapter`
- `StorageAdapter`
- `PodLifecycleAdapter`

Danbooru、rclone、sd-scripts、RunPod API固有処理をUIへ直接記述しない。

### 3.3 Gradio

- `app.py`へ全UIと処理を集中させない。
- タブまたは画面単位でUI構築関数を分離する。
- UIイベント関数は入力検証とサービス呼び出しに限定する。
- 数時間かかる処理をUIイベント内で同期実行しない。
- Gradioの一時状態だけを正とせず、永続化されたプロジェクト状態を正とする。
- 再読み込み後にジョブ状態を復元できるようにする。
- 大量画像一覧はサムネイル、ページング、遅延読込を考慮する。

## 4. ファイル・ストレージ

### 4.1 非破壊処理

- 原画像を上書きしない。
- 元の自動タグを上書きせず、`source_tags`、`detected_tags`、`final_tags`を分離する。
- 除外は物理削除ではなく状態変更を基本とする。
- 削除機能を実装する場合は確認と監査ログを必須とする。

### 4.2 安全な書き込み

- 一時ファイルへ書き込み、検証後に`os.replace`等で確定する。
- ファイル名をユーザー入力のまま利用しない。
- ディレクトリトラバーサルを防止する。
- ZIP展開時はZip Slipを防止する。
- ハッシュ値とファイルサイズを記録する。

### 4.3 Google Drive / rclone

- 通常の保存には`rclone copy`を使用する。
- `rclone sync`は原則禁止する。
- 同期元と同期先をログに記録するが、認証情報は出力しない。
- `subprocess`はシェル文字列ではなく引数配列を使う。
- 終了コード、標準エラー、再試行結果を確認する。
- 同期成功後に必要ファイルの存在またはチェック結果を検証する。
- 同期失敗時は完了扱いにせず、自動Terminateしない。

## 5. 学習プロセス

- sd-scriptsを独自に改変せず、外部バックエンドとして扱う。
- 実行コマンドは引数配列で組み立てる。
- 任意の追加シェル引数入力を許可しない。
- 許可されたパラメータだけを設定モデルから変換する。
- PID、開始時刻、終了時刻、終了コードを保存する。
- stdoutとstderrをファイルへ保存する。
- SIGINT、SIGTERMの順で安全な停止を試みる。
- 学習元モデル、dataset snapshot、設定、seed、ライブラリ版をRunへ記録する。
- 学習途中のファイルと最終成果物を明確に区別する。

## 6. タグ処理

- タグ比較時は正規化ルールを1か所に集約する。
- 自動タグと最終採用タグを分離する。
- タグ頻度は「タグが含まれる画像数」で集計する。
- 同一画像内で同じタグが重複しても1回として数える。
- タグ一覧は出現画像数の降順、同数の場合はタグ名昇順とする。
- チェックありを「残す」、チェックなしを「削除」とする。
- 初期状態は原則として全タグを残す。
- 削除適用前に対象画像数と差分を提示する。
- 元タグへ戻せるよう、タグ編集を非破壊で扱う。
- トリガーワードは設定に従って所定位置へ付与する。

## 7. エラー処理

- ユーザー向けメッセージと開発者向け詳細ログを分ける。
- 外部API、ファイルI/O、subprocessにはタイムアウトを設ける。
- リトライ可能なエラーと不可能なエラーを分ける。
- `except Exception: pass`は禁止する。
- 例外の再送出時は`raise ... from exc`を使用する。
- 失敗したジョブは原因、再試行可否、推奨対処を保存する。

## 8. ログ

- 構造化ログを推奨する。
- ログレベルを適切に使い分ける。
- Run ID、Project ID、Job IDをコンテキストとして含める。
- APIキー、トークン、Cookie、rclone configの内容を出力しない。
- 大量のタグや画像バイナリをそのままログ出力しない。
- subprocessのログは専用ファイルに保存する。

## 9. セキュリティ

- `.env`、APIキー、rclone設定、秘密鍵をGitへ含めない。
- Gradioは`0.0.0.0:7860`で待受可能だが、RunPod Proxy外へ不要なポートを公開しない。
- 自分専用でも認証またはRunPod側アクセス制御を利用する。
- URLダウンロードではSSRF対策を行う。
- アップロード画像は拡張子だけでなく実体形式を確認する。
- 任意コマンド、任意パス、任意URLを無検証で実行しない。
- RunPod APIキーは環境変数から読み、画面に表示しない。

## 10. RunPod Stop / Terminate

- デフォルトは自動終了無効とする。
- 自動終了は明示的なユーザー設定がある場合のみ行う。
- 学習成功、成果物検証成功、Google Drive同期成功、完了マニフェスト保存成功を必須条件とする。
- いずれかが失敗した場合はPodを維持する。
- Terminate要求前にログをflushし、短い猶予時間を設ける。
- StopとTerminateを別の列挙値として扱う。
- Terminate後にPodローカルデータが消えることをUIへ明示する。

## 11. テスト

### 11.1 必須テスト

- タグ頻度集計
- タグの保持・削除適用
- 完全重複・近似重複
- パス検証
- rcloneコマンド生成
- rclone失敗時にTerminateされないこと
- 学習コマンド生成
- 総step計算
- 設定シリアライズ・マイグレーション
- ジョブ状態遷移

### 11.2 テスト方針

- 外部API、rclone、sd-scripts、RunPod APIはモック可能にする。
- 単体テストで実GPUや実Google Driveを要求しない。
- 実環境結合テストは明示的なマーカーで分離する。
- バグ修正時は再現テストを先に追加する。
- テスト名から期待する振る舞いが分かるようにする。

## 12. 静的解析・フォーマット

以下を標準コマンドとする。

```bash
ruff format --check .
ruff check .
mypy src
pytest
```

Codexは変更後、可能な範囲ですべて実行する。実行できない項目がある場合は、理由を明記する。

## 13. Gitルール

### 13.1 管理対象

- ソースコード
- テスト
- マイグレーション
- シェルスクリプト
- 設定テンプレート
- ドキュメント
- 小容量のテストfixture

### 13.2 管理対象外

- `.env`
- rclone認証設定
- APIキー
- SDXLモデル
- LoRAファイル
- 学習画像
- キャプション実データ
- SQLite実運用DB
- cache
- logs
- outputs
- state
- Gradio一時ファイル

### 13.3 コミット

Conventional Commits形式を基本とする。

```text
feat(scope): 新しい機能を追加
fix(scope): 不具合を修正
refactor(scope): 動作を変えずに構造を改善
test(scope): テストを追加または更新
docs(scope): ドキュメントを更新
chore(scope): 開発環境や保守用ファイルを更新
```

1コミットは1つの論理的変更にする。生成物、大容量ファイル、秘密情報をコミットしない。

## 14. Codexの完了報告

Codexは対応後に必ず以下を返す。

- 実装概要
- 変更ファイル
- テスト結果
- 動作確認手順
- 残課題
- **推奨コミットメッセージ**

コミットメッセージは、そのままコピーできるコードブロックで提示する。
