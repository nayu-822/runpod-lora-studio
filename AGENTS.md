# AGENTS.md

## プロジェクト概要

このリポジトリは、RunPodのGPU Pod上で稼働する自分専用のGradioアプリケーション **RunPod LoRA Studio** を実装する。

本ツールは、SDXL LoRA向けの画像収集、画像選別、重複・品質検査、自動タグ付け、手動タグ選別、データセット作成、学習実行、成果物のGoogle Drive同期、および任意のPod自動停止・Terminateに対応する。

変更を行う前に、次の文書を順番に確認すること。

1. `RUNPOD_SDXL_LORA_TOOL_DESIGN_SPEC.md`
2. `DEVELOPMENT_PLAN.md`
3. `CODING_RULES.md`

文書間で指示が矛盾する場合は、次の優先順位に従うこと。

1. ユーザーから直近で明示された指示
2. `AGENTS.md`
3. `CODING_RULES.md`
4. `DEVELOPMENT_PLAN.md`
5. 設計仕様書

## 基本構成

- UI: Gradio Blocks
- 言語: Python 3.11以上
- 実行環境: RunPod GPU Pod
- 学習バックエンド: kohya-ss / sd-scripts
- プロセス実行: Gradioイベントハンドラー外で管理する`subprocess.Popen`
- 永続化: SQLiteおよびプロジェクトファイル
- コード配布: Gitリポジトリ
- クラウド保存: rclone経由のGoogle Drive
- 外部アクセス: ローカルPCのブラウザからRunPod HTTP Proxyへ接続

GPUを使用する重い処理を、ユーザーのローカルPCへ移さないこと。

## 保存先のルール

### Gitで管理するもの

- ソースコード
- テストコード
- スクリプト
- ドキュメント
- 設定テンプレート
- 小容量のテスト用fixture

### Google Driveへ保存するもの

以下はGoogle Driveへ永続保存すること。

- 学習元モデル
- 学習に採用した画像
- タグ編集後の最終キャプションテキスト
- データセット設定
- 学習設定
- 完成したLoRA
- 完成時のサンプル画像
- 学習ログ
- 完了マニフェスト

### RunPod内だけに一時保存するもの

学習処理中は、以下をRunPod内の作業領域に保持すること。

- latent cache
- text encoder cache
- 一時加工画像
- 学習再開用state
- 途中epochのLoRA
- 一時ログ
- ダウンロード途中のファイル

明示的な依頼がない限り、途中成果物をGoogle Driveへアップロードしないこと。

通常の成果物保存には`rclone copy`を使用し、`rclone sync`は使用しないこと。ローカル側に存在しないことを理由として、Google Drive上のファイルを削除してはならない。

## データ保護

- 原画像を上書きしないこと。
- タガーが生成した元のタグ情報を上書きしないこと。
- `source_tags`、`detected_tags`、`final_tags`を分離して保持すること。
- 画像の除外は、原則として物理削除ではなく復元可能な状態変更として扱うこと。
- 各学習開始前にデータセットスナップショットを作成すること。
- 再現性確保のため、ファイルハッシュと設定スナップショットを保存すること。

## タグ選別機能

LoRA学習前に、採用画像に含まれるタグを集計すること。

必須動作は以下のとおり。

- 同一画像内で同じタグが重複していても、出現回数は1回として数える。
- タグを、そのタグが含まれる画像数の降順で並べる。
- 出現画像数が同じ場合は、タグ名の昇順で並べる。
- 出現画像数と出現率を表示する。
- すべてのタグにチェックボックスを表示する。
- チェックありは「タグを残す」を意味する。
- チェックなしは「対象となる全画像の最終キャプションから削除する」を意味する。
- 保存済みの選択状態がない場合、初期状態はチェックありとする。
- 変更を反映する前に、削除対象とキャプション差分をプレビュー表示する。
- 操作を元に戻せるよう、元の自動タグを保持する。
- トリガーワードは通常タグと分離して管理し、暗黙的に削除しない。

## 長時間処理

画像取得、タグ付け、重複検査、品質検査、rclone転送、LoRA学習など、長時間かかる可能性がある処理をGradioの同期コールバックとして直接実行しないこと。

ジョブ管理層で、最低限以下を記録すること。

- Job ID
- Project ID
- 必要に応じてRun ID
- PID
- 状態
- 進捗
- 開始日時
- 終了日時
- 終了コード
- ログファイルのパス
- エラー概要

ブラウザとの接続が切れても、Podが稼働している限りジョブは継続すること。ブラウザ再接続後に、ジョブ状態を復元できるようにすること。

## subprocess実行ルール

- コマンドは文字列連結ではなく引数配列で渡すこと。
- 原則として`shell=True`を使用しないこと。
- ユーザーが設定できる引数は、許可リストまたは型付き設定モデルで検証すること。
- stdoutとstderrを取得・保存すること。
- 必要な処理にはタイムアウトを設定すること。
- プロセスの終了コードを保持すること。
- 強制終了の前に、安全な停止処理を試みること。

## Google Drive同期

sd-scriptsが正常終了しただけでは、学習Runを完了扱いにしないこと。

完了条件は以下のすべてを満たすこと。

1. 学習プロセスの終了コードが0である。
2. 最終LoRAが存在し、基本検証に成功する。
3. 必須となる画像、キャプション、設定、ログ、最終LoRAをGoogle Driveへコピーする。
4. rcloneの終了コードと転送結果を確認する。
5. 完了マニフェストを作成し、Google Driveへコピーする。

同期に失敗した場合は、次のように処理すること。

- Runを「同期失敗」または「完了待ち」として記録する。
- Podを稼働したままにする。
- StopまたはTerminateを自動実行しない。
- UIに再試行操作と、原因を判断できるエラー情報を表示する。

## RunPodのライフサイクル

自動停止機能は初期状態では無効とすること。

ユーザーが選択できる終了動作は以下とする。

- 何もしない
- 正常完了後にPodをStopする
- 正常完了後にPodをTerminateする

自動StopまたはTerminateは、必要な完了条件をすべて満たし、かつユーザーが学習開始前に明示的に有効化した場合だけ実行すること。

RunPod APIキーをUIやログへ表示してはならない。

## セキュリティ

- `.env`、APIキー、アクセストークン、Cookie、rclone設定、非公開モデル、データセット、生成LoRAをGitへコミットしないこと。
- ファイル名、URL、アーカイブ、モデルパスは信頼できない入力として扱うこと。
- ディレクトリトラバーサル、Zip Slip、SSRF、任意コマンド実行を防止すること。
- ファイル拡張子だけを信用せず、画像の実体形式を検証すること。
- 秘密情報をログへ出力しないこと。
- 不要な内部ポートを外部公開しないこと。

## コーディング時の要件

- `CODING_RULES.md`に従うこと。
- Gradio UIとサービス・アダプター層を分離すること。
- 公開APIには型ヒントを付けること。
- 型なし辞書より、dataclassまたはPydanticモデルを優先すること。
- 永続化する状態値にはEnumを使用すること。
- パス操作には`pathlib.Path`を使用すること。
- 振る舞いの変更や不具合修正にはテストを追加すること。
- 依頼と関係のないリファクタリングを行わないこと。
- 局所的な修正で対応できる場合、大規模なファイル書き換えを避けること。
- 動作やセットアップ方法を変更した場合、関連ドキュメントも更新すること。

## 動作確認

変更後は、利用可能な範囲で以下を実行すること。

```bash
ruff format --check .
ruff check .
mypy src
pytest
```

UIまたは外部サービス連携を変更した場合は、簡潔な手動確認手順も提示すること。

コマンドを実行できなかった場合は、実行できなかった理由を具体的に記載すること。根拠なく成功したと報告してはならない。

## 実装対応後に必ず報告する内容

実装作業の終了後は、必ず以下を順番に報告すること。

1. 実装・変更内容の概要
2. 変更したファイル一覧
3. 重要な実装上・設計上の判断
4. 実行したテストおよび確認コマンド
5. テストおよび確認結果
6. 未解決事項、既知の問題、残っているリスク
7. 手動確認手順
8. 推奨するGitコミットメッセージ

コミットメッセージは、そのままコピーして使用できる形式とし、可能な限りConventional Commits形式を使用すること。

日本語の例:

```text
feat(tagging): 出現回数順のタグ保持チェック機能を追加
```

変更内容が大きい場合は、任意でコミット本文も提示すること。

```text
feat(storage): 完成したLoRA成果物のGoogle Drive同期を追加

- 完成LoRA、採用画像、キャプション、設定、ログをrcloneでコピー
- 学習Runの完了前に転送結果を検証
- 同期失敗時はPodの自動Terminateを実行しない
```

ユーザーから明示的な依頼がない限り、`git commit`、`git push`、破壊的なGitコマンド、履歴の書き換えを実行しないこと。
